# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Training artifacts / checkpoints (large, regenerable)
isaaclab_pallet/runs/
*.pt
*.onnx

# OS
.DS_Store

# Note: sibling projects (Online-3D-BPP-PCT, templete code, palletizing_simulator,
# submission_*, UOS_robostack_1) are intentionally NOT committed here — this repo
# tracks the Isaac Lab port (isaaclab_pallet) and its porting docs only.
# `templete code/src/pct` and `Online-3D-BPP-PCT` remain runtime dependencies.
